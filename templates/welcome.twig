{% extends "base.twig" %}

{% block content %}
	{% if photos|length > 0 %}
		{% if is_granted('IS_AUTHENTICATED_FULLY') %}
			<div class="container">
				{% for photo in photos %}
					<div class="row">
						<div class="span1">
							<img src="/photoview/{{ photo.value.id }}{{ photo.value.extension }}" alt="{{ photo.value.title }}" />
						</div>
						<div class="span2">
							<p>{{ photo.value.title }}</p>
						</div>
						<div class="span9">
							<p></p>
						</div>
					</div>
				{% endfor %}
			</div>
		{% else %}
			<section class="edit-mode notebook-style">
				<div class="bg clearfix">
					<ul class="container">
						{% for photo in photos %}
							<li>
								<img src="/photoview/{{ photo.value.id }}{{ photo.value.extension }}" alt="{{ photo.value.title }}" />
								<div class="caption">
									<p>{{ photo.value.title }}</p>
								</div>
							</li>
						{% endfor %}
					</ul>
				</div>
			</section>
		{% endif %}
		<section class="edit-mode gallery-style">
			<div class="gridster bg clearfix">
				<ul class="container">
					{% set col = 1 %}
					{% set row = 1 %}
					{% for photo in photos %}
						<li class="gs_w thumbnail" data-sizex="1" data-sizey="1" data-col="{{ col }}" data-row="{{ row }}">
							<!-- <a href="/photo/{{ photo.value.id }}"> -->
							<img src="/photoview/{{ photo.value.id }}{{ photo.value.extension }}" alt="{{ photo.value.title }}" />
							<!-- </a> -->
							<div class="caption">
								<p>{{ photo.value.title }}</p>
							</div>
						</li>
						{% if col < 7 %}
							{% set col = col + 1 %}
						{% else %}
							{% set col = 1 %}
							{% set row = row + 1 %}
						{% endif %}
					{% endfor %}
				</ul>
			</div>
		</section>
	{% endif %}
{% endblock %}